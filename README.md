# Fleet Operations Reporting App

Clean SQL-first Python desktop app starter for direct fleet operations data entry, issue tracking, and report generation.

Default database: FleetOpsReporting
Default schema: ops

Run validation:

```powershell
python -m compileall src run_app.py tests
python -m unittest discover -s tests
```
